version: "3.9"

services:
  harmony-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: harmony-web
    restart: unless-stopped
    environment:
      PORT: 8090
    ports:
      - "8090:8090"
